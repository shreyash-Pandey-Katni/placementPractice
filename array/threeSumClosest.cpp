#include <iostream>
#include <vector>
#include <unordered_set>
#include <bits/stdc++.h>
using namespace std;
int threeSumClosest(vector<int> arr, int target)
{
    int n = arr.size();
    sort(arr.begin(), arr.end());
    int minDiff = INT_MAX;
    int i = 0;
    int j = n - 1;
    int k = n - 2;
    while
    // Your code goes here
}